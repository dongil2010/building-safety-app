# 운영 URL: https://frosty-king-12ef.dongilgujo2010.workers.dev
# 이 name 이 기존 Worker 와 같아야 `npx wrangler deploy` 가 새 워커를 만들지 않는다.
name = "frosty-king-12ef"
main = "ocr-proxy.js"
compatibility_date = "2026-08-01"

# OCR Secret (프록시에는 불필요, OCR 유지):
#   npx wrangler secret put GOOGLE_VISION_API_KEY
#
# 앱 도메인으로 CORS를 제한하고 싶으면:
# [vars]
# ALLOWED_ORIGIN = "https://dongil2010.github.io"
