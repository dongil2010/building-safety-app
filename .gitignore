# Android / Capacitor
node_modules/
www/
.capacitor/

# Android Studio / Gradle
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/.idea/
*.apk
*.aab
*.keystore
!android/app/debug.keystore

# OTA / Firebase CLI token (never commit)
.firebase-ci-token
.env.local

# GitHub Pages build output
_site/

# 한글 양식 작업 백업 (배포 대상 아님)
templates/*.bak
templates/*.hwpx.bak*
