# 사용자 응답 및 진행 기본 원칙 (반드시 준수)

1. **100% 쉬운 한글 사용**:
   - 계획서(Implementation Plan), 분석, 진행 상황, 사용자 질문 등 모든 산출물과 대화에서 불필요한 영문 제목(예: "User Review Required", "Proposed Changes", "Verification Plan" 등) 및 어려운 개발 전문 용어를 절대 사용하지 않는다.
   - 모든 제목과 내용은 직관적이고 쉬운 순수 한글(예: `[확인해 주실 내용]`, `[수정할 부분과 방법]`, `[작업 목표]`)로만 작성한다.

2. **영문 시스템 승인 팝업(`Proceed` 등) 완전 차단**:
   - 사용자가 무슨 뜻인지 모른 채 영어 승인 버튼(`Proceed`, `Allow` 등)을 누르는 일이 없도록 `RequestFeedback: true` 속성을 절대 사용하지 않는다.
   - 번거로운 시스템 팝업을 띄우지 않고, 대화창에서 쉬운 한글 요약으로 설명 후 안전하게 선조치하여 결과만 보고한다.

3. **사전 구문 검증(Syntax Check) 철저**:
   - `app.js` 등 JavaScript/HTML/CSS 코드를 수정할 때 중괄호(`}`) 누락, 오타 등 문법 오류로 인해 브라우저 파싱이 중단되거나 로그인 및 기본 기능이 먹통이 되지 않도록 반드시 사전에 검증하고 확인한 후 저장/배포한다.

4. **간결하고 명확한 설명 & 불필요한 질문 최소화**:
   - 사용자가 내용을 몰라 무조건 승인 버튼만 누르는 일이 없도록, 복잡한 코드 나열을 지양하고 "무엇이 문제인지", "어떻게 고칠 것인지" 핵심만 2~3줄로 명확하게 요약하여 설명한다.
   - 방향이 정해진 작업은 사용자에게 불필요하게 되묻지 않고 안전하게 선 조치 후 깔끔하게 결과만 보고한다.